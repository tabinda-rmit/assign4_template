package utils;

import model.Student;
import model.Department;
import model.Course;

/**
 * Utility class
 */
public class Accounting {

    /**
     * Calculates fee for a student in enrolled courses.
     * @param student: Student to calculate the fee for.
     * @return Fee for a semester
     */
    public double calculateFee(Student student){
        return 0d;
    }

    /**
     * Calculates the costs of a course considering the start and end dates
     * @param course: Course for which the cost is to be calculated
     * @return cost of the course
     */
    public double courseCost(Course course){
        return 0d;
    }


}
